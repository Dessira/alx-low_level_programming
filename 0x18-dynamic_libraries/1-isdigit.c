#include "main.h"
/**
 * _isdigit - checks if character is a digit.
 * @c: integer character
 * Return: 0 or 1 depending on output.
 */

int _isdigit(int c)
{
	if (c > 47 && c < 58)

	return (1);

	return (0);

}
